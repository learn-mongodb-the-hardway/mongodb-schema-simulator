package com.mtools.schemasimulator.schemas.shoppingcart

import com.mtools.schemasimulator.schemas.DataGenerator
import com.mtools.schemasimulator.schemas.DocumentTemplate

class ShoppingCartDataGenerator: DataGenerator {
    override fun generate(options: Map<String, Any>) {
    }
}

private class ProductGenerator {
    fun generate(template: DocumentTemplate) {
    }
}
